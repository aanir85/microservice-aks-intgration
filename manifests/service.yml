apiVersion: v1
kind: Service
metadata:
    name: microserviceaksintegrationimageaks
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: microserviceaksintegrationimageaks